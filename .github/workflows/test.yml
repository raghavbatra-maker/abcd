name: Test Workflow
on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Test message'
        required: false
        default: 'hello'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow triggered!"
